# دليل تفعيل واستخدام agent-auto

## نظرة عامة 📋

تم تفعيل **agent-auto** (وكيل التوجيه الذكي) بنجاح في منصة BSM. هذا الوكيل يعمل كموجه ذكي يحلل استعلامات المستخدم ويوجهها تلقائياً إلى الوكيل المناسب.

## التحديثات المنفذة ✅

### 1. إضافة إلى Registry
- **الملف**: `agents/registry.yaml`
- **الموقع**: السطر 354-386
- **التفاصيل**:
  - ID: `agent-auto`
  - الفئة: `conversational`
  - الدور: `router`
  - مستوى المخاطر: `low`
  - قابل للاختيار: `true`

### 2. إنشاء Copilot Agent
- **الملف**: `.github/agents/agent-auto.agent.md`
- **الغرض**: تمكين استخدام الوكيل مع GitHub Copilot
- **المحتوى**: تعليمات التوجيه وقواعد التصنيف

### 3. تحديث الوثائق
- **الملف**: `.github/agents/README.md`
- **التغيير**: إضافة agent-auto إلى قائمة الوكلاء

## كيفية الاستخدام 🚀

### 1. عبر API

```bash
curl -X POST http://localhost:3000/api/agents/run \
  -H "Content-Type: application/json" \
  -d '{
    "agentId": "agent-auto",
    "input": "ما هي المادة 77 من نظام الشركات؟",
    "payload": {}
  }'
```

### 2. عبر واجهة الدردشة

1. افتح `/chat` في المتصفح
2. اختر **agent-auto** من القائمة المنسدلة
3. اكتب استفسارك باللغة العربية أو الإنجليزية
4. الوكيل سيوجه الاستفسار تلقائياً للوكيل المناسب

### 3. عبر GitHub Copilot

```
@agent-auto ما هي إجراءات الحوكمة المطلوبة؟
```

## قواعد التوجيه 📍

يقوم agent-auto بتحليل الاستفسار وتوجيهه حسب المحتوى:

### 1. legal-agent (الوكيل القانوني)
**يُوجَّه إليه عند**:
- أسئلة عن الأنظمة والقوانين
- استفسارات عن العقود والتعاميم
- أسئلة عن الإفلاس والشركات
- أسئلة عن نظام العمل

**مثال**:
```
"ما هي شروط تأسيس شركة مساهمة؟"
```

### 2. governance-agent (وكيل الحوكمة)
**يُوجَّه إليه عند**:
- أسئلة عن الحوكمة والامتثال
- استفسارات عن السياسات الداخلية
- أسئلة عن اللوائح التنظيمية
- استفسارات عن إدارة المخاطر

**مثال**:
```
"ما هي سياسة الحوكمة للشركات العائلية؟"
```

### 3. my-agent (وكيل النظام)
**يُوجَّه إليه عند**:
- طلبات تنفيذ أوامر التيرمنال
- إدارة النظام والصيانة
- تشغيل الاختبارات
- فحص صحة النظام

**مثال**:
```
"شغل الاختبارات"
"فحص حالة النظام"
```

### 4. Direct (مباشر)
**يُوجَّه إليه عند**:
- الأسئلة العامة
- الاستفسارات التقنية
- طلبات الترجمة
- الأسئلة غير المتخصصة

**مثال**:
```
"ما هو أفضل إطار عمل لتطوير الويب؟"
```

## المواصفات التقنية 🔧

### معلومات الوكيل
- **ID**: `agent-auto`
- **الاسم**: Smart Router Agent
- **النموذج**: GPT-4o-mini
- **المزود**: OpenAI
- **الإصدار**: 1.0

### إعدادات الأمان
- **الوضع**: Safe (آمن)
- **يتطلب موافقة**: لا
- **مستوى المخاطر**: منخفض
- **السياقات المسموحة**: chat, api, mobile

### القدرات
- ✅ تصنيف النية (Intent Classification)
- ✅ توجيه الوكلاء (Agent Routing)
- ✅ دعم متعدد اللغات (عربي/إنجليزي)
- ✅ فهم السياق (Context Awareness)

## الأداء والمراقبة 📊

### Health Check
```bash
curl http://localhost:3000/api/agents/agent-auto/health
```

**معلومات المراقبة**:
- **Endpoint**: `/api/agents/agent-auto/health`
- **Interval**: 60 seconds
- **Timeout**: 30 seconds

### السجلات
```bash
# عرض سجلات الوكيل
tail -f logs/agents.log | grep agent-auto

# عرض سجلات التدقيق
tail -f logs/audit.log | grep agent-auto
```

## استكشاف الأخطاء 🔍

### المشكلة: الوكيل لا يظهر في القائمة

**الحل**:
```bash
# 1. تحقق من التسجيل
grep "agent-auto" agents/registry.yaml

# 2. تحقق من الفهرس
cat data/agents/index.json | jq '.agents[] | select(. | contains("agent-auto"))'

# 3. أعد تشغيل الخادم
npm run dev
```

### المشكلة: خطأ في التحقق من الصحة

**الحل**:
```bash
# تشغيل التحقق
npm run validate

# التحقق من Registry فقط
npm run validate:registry
```

### المشكلة: الوكيل لا يستجيب

**الحل**:
```bash
# 1. تحقق من المتغيرات البيئية
echo $OPENAI_BSM_KEY

# 2. تحقق من السجلات
tail -f logs/app.log

# 3. اختبار الاتصال
npm run health:detailed
```

## أمثلة الاستخدام 💡

### مثال 1: استعلام قانوني
```json
{
  "agentId": "agent-auto",
  "input": "ما هي عقوبة التهرب الضريبي في النظام السعودي؟",
  "payload": {}
}
```

**النتيجة المتوقعة**: التوجيه إلى `legal-agent`

### مثال 2: استعلام حوكمة
```json
{
  "agentId": "agent-auto",
  "input": "كيف نطبق معايير الحوكمة في الشركة؟",
  "payload": {}
}
```

**النتيجة المتوقعة**: التوجيه إلى `governance-agent`

### مثال 3: أمر تنفيذي
```json
{
  "agentId": "agent-auto",
  "input": "شغل npm test",
  "payload": {}
}
```

**النتيجة المتوقعة**: التوجيه إلى `my-agent`

### مثال 4: استعلام عام
```json
{
  "agentId": "agent-auto",
  "input": "اشرح لي مفهوم Blockchain",
  "payload": {}
}
```

**النتيجة المتوقعة**: إجابة مباشرة (direct)

## التكامل مع الأنظمة الأخرى 🔗

### 1. تكامل مع Chat UI
الوكيل متاح تلقائياً في:
- `/chat` - واجهة الدردشة الأساسية
- `/admin` - لوحة التحكم الإدارية
- Mobile apps - تطبيقات الهاتف المحمول

### 2. تكامل مع GitHub Actions
```yaml
- name: Run agent-auto
  run: |
    curl -X POST ${{ secrets.API_URL }}/api/agents/run \
      -H "Content-Type: application/json" \
      -d '{"agentId": "agent-auto", "input": "${{ github.event.inputs.query }}"}'
```

### 3. تكامل مع Telegram Bot
الوكيل يستجيب تلقائياً لرسائل Telegram عبر webhook.

## الإحصائيات 📈

### قبل التفعيل
- عدد الوكلاء في Registry: 16
- عدد Copilot Agents: 10
- الوكلاء القابلة للاختيار: 8

### بعد التفعيل
- عدد الوكلاء في Registry: 17 ✅
- عدد Copilot Agents: 11 ✅
- الوكلاء القابلة للاختيار: 9 ✅

## الأمان والحوكمة 🔒

### مستوى الأمان
- **Risk Level**: Low
- **Approval Required**: No
- **Auto Start**: False (disabled)
- **Selectable**: True

### الصلاحيات
- ✅ قراءة الاستعلامات
- ✅ تصنيف النية
- ✅ توجيه الطلبات
- ❌ تعديل النظام
- ❌ تنفيذ الأوامر
- ❌ الوصول المباشر للبيانات

### الامتثال
- ✅ مُسجَّل في Registry v2.0
- ✅ جميع الحقول المطلوبة موجودة
- ✅ Governance fields محدثة
- ✅ Validation passed
- ✅ Saffio Anti-Duplication System

## التطوير المستقبلي 🚀

### المخطط للإصدارات القادمة
1. **v1.1**: إضافة تعلم آلي لتحسين دقة التوجيه
2. **v1.2**: دعم سياقات متعددة في استعلام واحد
3. **v1.3**: تكامل مع Knowledge Base للتوجيه الذكي
4. **v2.0**: توجيه متعدد الوكلاء (Multi-Agent Routing)

### الميزات المقترحة
- [ ] Confidence scoring للتوجيه
- [ ] A/B testing للخوارزميات
- [ ] Analytics dashboard للتوجيه
- [ ] Custom routing rules
- [ ] Fallback strategies

## الدعم والمساعدة 💬

### الحصول على المساعدة
1. **الوثائق**: راجع `CLAUDE.md` للتفاصيل الكاملة
2. **الأمثلة**: تحقق من `examples/` directory
3. **السجلات**: فحص `logs/agents.log`
4. **GitHub Issues**: افتح issue جديدة مع tag `agent-auto`

### معلومات الاتصال
- **Repository**: MOTEB1989/BSM
- **Branch**: copilot/activate-proxy-service
- **Maintainer**: BSU Platform Team
- **Last Updated**: 2026-02-20

---

**Status**: ✅ **Active & Operational**  
**Registry**: agents/registry.yaml (Line 354-386)  
**Version**: 2.0  
**Agent Count**: 17/17

**تم التفعيل بنجاح! 🎉**
