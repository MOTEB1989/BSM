#!/usr/bin/env bash
set -euo pipefail

# WHY: Prevent low-quality or broken changes from being committed by enforcing the same gate used in CI.
echo "🔎 Running unified quality gate (npm run ci:check)..."
npm run ci:check
