{
  "TELEGRAM_BOT_TOKEN": {
    "description": "Telegram bot token for ORBIT",
    "usedBy": ["telegram-agent"],
    "required": true,
    "sensitive": true
  },
  "ORBIT_ADMIN_CHAT_IDS": {
    "description": "Admin chat IDs for privileged commands",
    "usedBy": ["telegram-agent"],
    "required": true,
    "sensitive": false
  },
  "TELEGRAM_WEBHOOK_SECRET": {
    "description": "Webhook secret token",
    "usedBy": ["telegram-agent"],
    "required": true,
    "sensitive": true
  }
}
