# خطة عمل: إدارة طلبات السحب المفتوحة

**التاريخ:** 2026-02-13  
**الحالة الحالية:** 30 طلب سحب مفتوح  
**الهدف:** تصنيف، معالجة، ودمج/إغلاق طلبات السحب بشكل منهجي

---

## 📋 الوضع الحالي

### المشاكل المحددة:
1. **30 طلب سحب مفتوح** - عدد كبير يحتاج إدارة
2. **تعارضات غير محلولة** - بعض PRs تحتوي على تعارضات merge
3. **عدم وجود تصنيف واضح** - صعوبة معرفة حالة كل PR
4. **طلبات قديمة (stale)** - PRs لم تُحدث منذ فترة طويلة

---

## ✅ الحلول المطبقة

### 1. Workflow للإدارة التلقائية
**الملف:** `.github/workflows/pr-management.yml`

**الإمكانيات:**
- تصنيف تلقائي لجميع PRs
- دمج تلقائي للـ PRs الجاهزة
- إغلاق PRs القديمة (+30 يوم)
- إضافة تسميات (labels) تلقائياً

**التسميات:**
- ✅ `ready` - جاهز للدمج
- ⚠️ `needs-changes` - يحتاج تعديلات
- 🚫 `blocked` - محظور
- 🕰️ `stale` - قديم (14+ يوم)
- ⚔️ `conflicts` - يحتوي على تعارضات
- 🤖 `auto-merge` - سيُدمج تلقائياً

### 2. PR Merge Agent محسّن
**الملف:** `src/agents/PRMergeAgent.js`

**التحسينات:**
- فحص التعارضات تلقائياً
- فحص حالة draft
- فحص القدم (staleness)
- فحص حالة CI
- فحص عدد الموافقات
- توصيات واضحة لكل حالة

### 3. سكريبتات التحليل

#### pr-status-check.js (خفيف وسريع)
```bash
GITHUB_TOKEN=xxx node scripts/pr-status-check.js
```
- يستخدم فقط Node.js built-ins
- تحليل سريع للحالة
- لا يحتاج dependencies إضافية

#### pr-manager.js (تحليل شامل)
```bash
# تثبيت dependency أولاً
npm install @octokit/rest

GITHUB_TOKEN=xxx node scripts/pr-manager.js
```
- تحليل مفصل لكل PR
- تقرير شامل مع إحصائيات
- توصيات عملية

### 4. دليل شامل
**الملف:** `docs/PR-MANAGEMENT-GUIDE.md`
- شرح المعايير والإجراءات
- أفضل الممارسات
- استكشاف الأخطاء

---

## 🚀 خطوات التنفيذ الفورية

### المرحلة 1: التحليل (تم ✅)
```bash
# تحليل سريع
GITHUB_TOKEN=$GITHUB_TOKEN node scripts/pr-status-check.js
```

### المرحلة 2: التصنيف (الآن)
```bash
# تصنيف جميع PRs وإضافة labels
gh workflow run pr-management.yml -f action=triage
```
أو من GitHub UI:
1. Actions → PR Management & Auto-Merge
2. Run workflow
3. اختر: `triage`

### المرحلة 3: المعالجة (حسب الأولوية)

#### أ. PRs ذات التعارضات (عاجل)
**الإجراء:** حل التعارضات يدوياً
```bash
# لكل PR متعارض:
git checkout main
git pull origin main
git checkout -b fix-pr-XXX
gh pr checkout XXX
git merge main
# حل التعارضات...
git add .
git commit -m "Resolve conflicts with main"
git push
```

#### ب. PRs الجاهزة (دمج فوراً)
```bash
# دمج PRs المصنفة بـ 'ready'
gh workflow run pr-management.yml -f action=auto-merge-ready
```

#### ج. PRs القديمة (مراجعة ثم إغلاق)
1. مراجعة كل PR قديم (+30 يوم)
2. تقرر: تحديث أم إغلاق
3. للإغلاق التلقائي:
```bash
gh workflow run pr-management.yml -f action=close-stale
```

#### د. PRs تحتاج مراجعة
**الإجراء:** تعيين مراجعين
```bash
# لكل PR:
gh pr review XXX --approve  # إذا كانت جيدة
# أو
gh pr review XXX --request-changes --body "الملاحظات..."
```

---

## 📊 معايير القبول

### PR جاهز للدمج يجب أن:
- ✅ يحصل على موافقة واحدة على الأقل
- ✅ تنجح جميع فحوصات CI
- ✅ لا يحتوي على تعارضات
- ✅ ليس draft
- ✅ محدث خلال آخر 14 يوم

### PR يُغلق تلقائياً إذا:
- 🕰️ لم يُحدّث منذ 30+ يوم
- 🏷️ مُصنف بـ `stale`
- ❌ لا توجد أسباب للإبقاء عليه

---

## 🔄 الصيانة المستمرة

### يومياً (تلقائي):
- تشغيل workflow التصنيف في 02:00 UTC
- تحديث التسميات
- إنشاء تقرير

### أسبوعياً (يدوي):
```bash
# مراجعة الحالة
gh workflow run pr-management.yml -f action=triage

# دمج PRs الجاهزة
gh workflow run pr-management.yml -f action=auto-merge-ready
```

### شهرياً (يدوي):
```bash
# مراجعة PRs القديمة
gh workflow run pr-management.yml -f action=close-stale
```

---

## 📈 مؤشرات النجاح

| المؤشر | الحالي | الهدف (شهر) | الهدف (3 أشهر) |
|--------|--------|-------------|-----------------|
| عدد PRs المفتوحة | 30 | < 15 | < 10 |
| PRs ذات تعارضات | ؟ | 0 | 0 |
| PRs قديمة (+14 يوم) | ؟ | < 5 | < 3 |
| متوسط وقت الدمج | ؟ | < 3 أيام | < 2 أيام |

---

## 🎯 التوصيات النهائية

### للفريق:
1. **مراجعة يومية** لـ PRs الجديدة
2. **استجابة سريعة** للملاحظات (خلال 48 ساعة)
3. **حل التعارضات** فور ظهورها
4. **استخدام draft** للـ PRs غير الجاهزة

### للأتمتة:
1. ✅ تفعيل الـ workflow اليومي (تلقائي)
2. 📅 جدولة مراجعة أسبوعية
3. 🔄 تحديث معايير التصنيف حسب الحاجة

### الخطوات التالية الفورية:
1. ✅ **تشغيل التصنيف:**
   ```bash
   gh workflow run pr-management.yml -f action=triage
   ```

2. ⏳ **انتظر النتائج** (5-10 دقائق)

3. 📊 **مراجعة التقرير** في GitHub Actions

4. 🎯 **تنفيذ الإجراءات** حسب الأولوية:
   - حل التعارضات أولاً
   - دمج الجاهزة ثانياً
   - مراجعة القديمة أخيراً

---

## 📞 الدعم

- **الوثائق:** `docs/PR-MANAGEMENT-GUIDE.md`
- **سكريبتات:** `scripts/pr-status-check.js`, `scripts/pr-manager.js`
- **Workflow:** `.github/workflows/pr-management.yml`

---

**آخر تحديث:** 2026-02-13  
**الحالة:** جاهز للتنفيذ ✅
