# 🚀 دليل البدء السريع - إدارة طلبات السحب

## الوضع الحالي
📊 **30 طلب سحب مفتوح** في المستودع يحتاج إلى تصنيف وإدارة

## ⚡ الإجراء الفوري (5 دقائق)

### 1️⃣ تصنيف جميع PRs
```bash
gh workflow run pr-management.yml -f action=triage
```
**النتيجة:** سيتم تصنيف جميع الـ 30 PR وإضافة التسميات المناسبة

### 2️⃣ مراقبة التنفيذ
```bash
gh run list --workflow=pr-management.yml -L 1
```
**المدة:** 5-10 دقائق لإكمال التصنيف

### 3️⃣ مراجعة النتائج
اذهب إلى: https://github.com/LexBANK/BSM/pulls

ستجد التسميات التالية على PRs:
- ✅ `ready` - جاهز للدمج فوراً
- ⚠️ `needs-changes` - يحتاج تعديلات
- ⚔️ `conflicts` - يحتوي تعارضات
- 🕰️ `stale` - قديم (14+ يوم)
- 🚫 `blocked` - محظور

## 📅 الإجراءات التالية (حسب الأولوية)

### أولاً: حل التعارضات ⚔️
```bash
# لكل PR متعارض:
gh pr checkout <PR_NUMBER>
git merge main
# حل التعارضات...
git add .
git commit -m "Resolve conflicts"
git push
```

### ثانياً: دمج PRs الجاهزة ✅
```bash
gh workflow run pr-management.yml -f action=auto-merge-ready
```
**النتيجة:** سيتم دمج جميع PRs المصنفة بـ `ready` تلقائياً

### ثالثاً: مراجعة PRs القديمة 🕰️
1. افتح كل PR مصنف بـ `stale`
2. قرر: تحديث أم إغلاق
3. للإغلاق التلقائي للقديم جداً (+30 يوم):
```bash
gh workflow run pr-management.yml -f action=close-stale
```

### رابعاً: معالجة PRs تحتاج تعديلات ⚠️
- أضف مراجعين
- اطلب تعديلات واضحة
- تابع التقدم

## 🤖 الأتمتة المستمرة

### تم تفعيلها تلقائياً!
- **يومياً في 02:00 UTC**: تصنيف تلقائي لجميع PRs
- **النتيجة**: تحديث التسميات وإنشاء تقرير

### للتحكم اليدوي:
```bash
# تحليل فقط (بدون تغييرات)
gh workflow run pr-management.yml -f action=analyze

# تصنيف كامل
gh workflow run pr-management.yml -f action=triage

# دمج تلقائي
gh workflow run pr-management.yml -f action=auto-merge-ready

# إغلاق القديمة
gh workflow run pr-management.yml -f action=close-stale
```

## 📊 فحص الحالة

### فحص سريع (بدون dependencies):
```bash
GITHUB_TOKEN=xxx node scripts/pr-status-check.js
```

### تحليل شامل:
```bash
npm install @octokit/rest
GITHUB_TOKEN=xxx node scripts/pr-manager.js
```

## 🎯 الأهداف المتوقعة

| الفترة | الهدف |
|--------|--------|
| بعد أسبوع | < 20 PR مفتوح، 0 تعارضات |
| بعد شهر | < 15 PR مفتوح، < 3 PRs قديمة |
| بعد 3 أشهر | < 10 PR مفتوح، متوسط دمج < 2 يوم |

## 📚 للمزيد من المعلومات

- **الدليل الكامل:** `docs/PR-MANAGEMENT-GUIDE.md`
- **خطة العمل:** `docs/PR-ACTION-PLAN.md`
- **الملخص التقني:** `docs/PR-MANAGEMENT-IMPLEMENTATION-SUMMARY.md`

## 💡 نصائح

1. **ابدأ بالتصنيف أولاً** - سيعطيك صورة واضحة
2. **حل التعارضات فوراً** - تمنع تراكم المشاكل
3. **ادمج الجاهز يومياً** - يحافظ على تدفق العمل
4. **راجع القديمة أسبوعياً** - للبقاء منظماً

## ✅ قائمة التحقق

- [ ] تشغيل التصنيف الأول
- [ ] انتظار النتائج (5-10 دق)
- [ ] مراجعة التسميات على GitHub
- [ ] حل التعارضات
- [ ] دمج PRs الجاهزة
- [ ] مراجعة PRs القديمة
- [ ] إعداد جدول مراجعة أسبوعي

---

**الحالة:** ✅ جاهز للتنفيذ  
**الوقت المتوقع:** 30 دقيقة للإعداد الأولي  
**الصيانة:** 15 دقيقة أسبوعياً
