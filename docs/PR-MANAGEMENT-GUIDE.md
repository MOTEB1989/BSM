# دليل إدارة طلبات السحب (PR Management Guide)

## نظرة عامة

هذا الدليل يوضح كيفية إدارة طلبات السحب المفتوحة في مستودع BSM بشكل فعال وآلي.

## المشكلة

كان لدينا 30 طلب سحب مفتوح مع:
- تعارضات غير محلولة
- عدم وجود آلية دمج تلقائية فعالة
- طلبات قديمة (stale) لم يتم إغلاقها
- عدم وجود تصنيف واضح لحالة كل PR

## الحل المطبق

### 1. سير عمل إدارة PRs التلقائي

تم إنشاء workflow جديد: `.github/workflows/pr-management.yml`

#### الإجراءات المتاحة:

1. **تحليل (analyze)**: تحليل وتصنيف جميع الـ PRs المفتوحة
2. **تصنيف (triage)**: تصنيف PRs وإضافة التسميات المناسبة
3. **دمج تلقائي (auto-merge-ready)**: دمج PRs الجاهزة تلقائياً
4. **إغلاق القديمة (close-stale)**: إغلاق PRs القديمة (+30 يوم)

#### التسميات المستخدمة:

- `ready` ✅: PR جاهز للدمج
- `needs-changes` ⚠️: يحتاج تعديلات
- `blocked` 🚫: محظور لسبب ما
- `stale` 🕰️: لم يشهد نشاطاً لمدة 14+ يوم
- `conflicts` ⚔️: يحتوي على تعارضات
- `auto-merge` 🤖: سيتم دمجه تلقائياً

### 2. معايير التصنيف

#### PR جاهز للدمج (`ready`):
- ✅ لديه موافقة (approval) واحدة على الأقل
- ✅ جميع فحوصات CI ناجحة
- ✅ لا توجد تعارضات
- ✅ ليس draft

#### PR يحتاج تعديلات (`needs-changes`):
- فحوصات CI فاشلة
- طلب تعديلات من المراجعين
- لا يوجد موافقات كافية

#### PR محظور (`blocked`):
- يحتوي على تسمية blocking (dependencies, product-decision)
- يعتمد على PRs أخرى

#### PR قديم (`stale`):
- لم يتم تحديثه منذ 14+ يوم

#### PR يحتوي على تعارضات (`conflicts`):
- `mergeable === false`
- يحتاج حل تعارضات يدوياً

### 3. الجدولة التلقائية

- يتم تشغيل التصنيف (triage) تلقائياً كل يوم في الساعة 02:00 UTC
- يمكن تشغيل أي إجراء يدوياً من GitHub Actions

### 4. كيفية الاستخدام

#### التشغيل اليدوي:

1. اذهب إلى GitHub Actions → PR Management & Auto-Merge
2. اضغط "Run workflow"
3. اختر الإجراء المطلوب:
   - `analyze`: للتحليل فقط
   - `triage`: للتصنيف وإضافة التسميات
   - `auto-merge-ready`: لدمج PRs الجاهزة
   - `close-stale`: لإغلاق PRs القديمة (+30 يوم)

#### عبر سطر الأوامر:

```bash
# تحليل PRs
gh workflow run pr-management.yml -f action=analyze

# تصنيف PRs
gh workflow run pr-management.yml -f action=triage

# دمج PRs الجاهزة
gh workflow run pr-management.yml -f action=auto-merge-ready

# إغلاق PRs القديمة
gh workflow run pr-management.yml -f action=close-stale
```

### 5. سكريبت إدارة PRs المحلي

تم إنشاء `scripts/pr-manager.js` لتحليل PRs محلياً:

```bash
# تثبيت التبعيات (إذا لزم الأمر)
npm install @octokit/rest

# تشغيل السكريبت
GITHUB_TOKEN=your_token node scripts/pr-manager.js
```

يقوم السكريبت بـ:
- تحليل جميع PRs المفتوحة
- تصنيفها حسب الحالة
- إنشاء تقرير مفصل
- تقديم توصيات للإجراءات المطلوبة

## أفضل الممارسات

### للمطورين:

1. **حافظ على PRs محدثة**: قم بدمج التغييرات من main بانتظام
2. **حل التعارضات فوراً**: عند ظهور تعارضات، قم بحلها في أسرع وقت
3. **استجب للمراجعات**: عند طلب تعديلات، قم بتنفيذها خلال 7 أيام
4. **استخدم draft**: إذا كان PR لا يزال قيد العمل

### للمراجعين:

1. **راجع بسرعة**: حاول مراجعة PRs خلال 48 ساعة
2. **كن واضحاً**: اذكر بوضوح ما يحتاج تعديل
3. **استخدم الموافقة**: عند رضاك الكامل، استخدم "Approve"

### لمدراء المشروع:

1. **راقب التقارير**: راجع تقارير PR Management يومياً
2. **تعامل مع القديمة**: قم بمراجعة PRs القديمة أسبوعياً
3. **دمج السريع**: ادمج PRs الجاهزة في أسرع وقت

## استكشاف الأخطاء

### PR لا يتم دمجه تلقائياً

تحقق من:
- [ ] هل لديه تسمية `ready`?
- [ ] هل جميع فحوصات CI ناجحة?
- [ ] هل لديه موافقة واحدة على الأقل?
- [ ] هل لا يحتوي على تعارضات?

### PR تم تصنيفه بشكل خاطئ

- قم بإزالة التسمية الخاطئة يدوياً
- قم بتشغيل workflow مرة أخرى
- تحقق من معايير التصنيف أعلاه

### PR قديم لكن لا يزال مطلوباً

- قم بإضافة تعليق جديد على PR
- قم بإزالة تسمية `stale`
- أضف تسمية `priority-high` لمنع الإغلاق

## الصيانة

### مراجعة أسبوعية

قم بتشغيل:
```bash
gh workflow run pr-management.yml -f action=triage
```

### تنظيف شهري

قم بمراجعة PRs القديمة (+30 يوم) وتقرر:
- إغلاق: إذا لم تعد مطلوبة
- تحديث: إذا لا تزال مطلوبة

### تحديث المعايير

إذا احتجت لتعديل معايير التصنيف، قم بتعديل:
- `.github/workflows/pr-management.yml`
- `scripts/pr-manager.js`

## الخلاصة

هذا النظام يوفر:
- ✅ تصنيف تلقائي لجميع PRs
- ✅ دمج تلقائي للـ PRs الجاهزة
- ✅ إدارة PRs القديمة
- ✅ تقارير واضحة عن حالة المستودع
- ✅ تقليل العبء اليدوي على الفريق

## المصادر

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [GitHub REST API](https://docs.github.com/en/rest)
- [Best Practices for Pull Requests](https://docs.github.com/en/pull-requests)
