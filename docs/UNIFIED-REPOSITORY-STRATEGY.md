# استراتيجية المستودع الموحد (Unified Repository Strategy)

## نظرة عامة (Overview)

تم توحيد جميع مكونات منصة BSU/LexBANK في مستودع واحد رئيسي:
- **المستودع الرئيسي**: `MOTEB1989/BSM`
- **المستودع القديم**: `LexBANK/BSM` (يتم إعادة التوجيه)

All components of the BSU/LexBANK platform are now unified in a single repository:
- **Main Repository**: `MOTEB1989/BSM`
- **Legacy Repository**: `LexBANK/BSM` (redirected)

---

## الهيكل الجديد (New Structure)

```
BSM/ (المستودع الموحد / Unified Repository)
├── 📁 .github/
│   ├── copilot/
│   │   └── mcp.json              # إعدادات GitHub Copilot
│   └── workflows/                # سير عمل CI/CD
│
├── 📁 frontend/                  # الواجهة الأمامية (GitHub Pages)
│   ├── index.html                # واجهة الدردشة الموحدة
│   ├── app.js                    # منطق تطبيق Vue 3
│   ├── manifest.json             # دعم PWA
│   ├── assets/                   # الملفات الثابتة
│   └── README.md                 # توثيق الواجهة
│
├── 📁 src/                       # الخادم (Backend - Render.com)
│   ├── server.js                 # نقطة دخول الخادم
│   ├── app.js                    # إعداد Express
│   ├── agents/                   # وكلاء الذكاء الاصطناعي
│   ├── api/                      # عملاء API متعددين
│   ├── routes/                   # مسارات API
│   ├── services/                 # منطق الأعمال
│   ├── middleware/               # وسيطات Express
│   └── ...                       # مكونات أخرى
│
├── 📁 mcp-servers/               # تكامل Copilot (MCP)
│   ├── bsu-agent-server.js       # خادم MCP
│   ├── package.json              # تبعيات MCP
│   └── README.md                 # توثيق MCP
│
├── 📁 shared/                    # موارد مشتركة
│   └── config.js                 # التكوين الموحد
│
├── 📁 data/                      # بيانات النظام
│   ├── agents/                   # تعريفات الوكلاء (YAML)
│   └── knowledge/                # قاعدة المعرفة
│
├── 📁 docs/                      # التوثيق والصفحات
│   └── ...
│
├── 📁 scripts/                   # نصوص البناء والتحقق
│   └── ...
│
└── package.json                  # التبعيات الرئيسية
```

---

## المكونات الرئيسية (Main Components)

### 1. التكوين الموحد (Unified Configuration)

📄 **الملف**: `shared/config.js`

يحتوي على جميع إعدادات النظام:
- عناوين URL (frontend, backend, repo)
- إعدادات الوكلاء (GPT, Gemini, Claude, Perplexity, Kimi)
- إعدادات الأمان (CORS, CSP)
- إعدادات MCP (Copilot)
- إعدادات الواجهة (UI)

**استخدام في الواجهة الأمامية**:
```javascript
const config = window.BSMConfig;
```

**استخدام في الخادم**:
```javascript
const config = require('../shared/config.js');
```

### 2. الواجهة الأمامية (Frontend)

📂 **المجلد**: `frontend/`

- **النوع**: تطبيق Vue 3 مستقل
- **النشر**: GitHub Pages
- **الرابط**: `https://moteb1989.github.io/BSM/frontend/`
- **الميزات**:
  - دعم 5 وكلاء ذكاء اصطناعي
  - واجهة ثنائية اللغة (عربي/إنجليزي)
  - دعم PWA
  - تخزين المحادثات محليًا
  - تصميم متجاوب

### 3. الخادم (Backend)

📂 **المجلد**: `src/`

- **النوع**: خادم Node.js/Express
- **النشر**: Render.com
- **الرابط**: `https://sr-bsm.onrender.com`
- **الميزات**:
  - دعم متعدد للموفرين
  - حماية Circuit Breaker
  - تسجيل منظم
  - معالجة الأخطاء الشاملة
  - حدود المعدل

### 4. خادم MCP (Copilot Integration)

📂 **المجلد**: `mcp-servers/`

- **النوع**: خادم Model Context Protocol
- **التكامل**: GitHub Copilot
- **الإصدار**: 2.0.0
- **الميزات**:
  - 6 أدوات للدردشة
  - 3 موارد للنظام
  - دعم stdio transport
  - تكامل API موحد

---

## عناوين URL الرئيسية (Main URLs)

| المكون | URL |
|-------|-----|
| الواجهة الأمامية | https://moteb1989.github.io/BSM/frontend/ |
| الخادم API | https://sr-bsm.onrender.com/api |
| المستودع الرئيسي | https://github.com/MOTEB1989/BSM |
| المستودع القديم | https://github.com/LexBANK/BSM (redirect) |

---

## الوكلاء المتاحون (Available Agents)

| الوكيل | الموفر | النموذج | نقطة النهاية |
|-------|--------|---------|--------------|
| GPT-4 | OpenAI | gpt-4 | `/api/chat/direct` |
| Gemini | Google | gemini-2.0-flash-exp | `/api/chat/gemini` |
| Claude | Anthropic | claude-3-5-sonnet | `/api/chat/claude` |
| Perplexity | Perplexity | sonar-pro | `/api/chat/perplexity` |
| Kimi | Moonshot | kimi-latest | `/api/chat/kimi` |

---

## التثبيت والإعداد (Installation & Setup)

### 1. استنساخ المستودع (Clone Repository)

```bash
git clone https://github.com/MOTEB1989/BSM.git
cd BSM
```

### 2. تثبيت التبعيات (Install Dependencies)

```bash
# تثبيت تبعيات الخادم
npm install

# تثبيت تبعيات MCP (تلقائي)
npm run mcp:install
```

### 3. إعداد البيئة (Environment Setup)

```bash
cp .env.example .env
# قم بتحرير .env وإضافة مفاتيح API الخاصة بك
```

### 4. تشغيل الخادم (Run Server)

```bash
# التطوير
npm run dev

# الإنتاج
npm start
```

### 5. تشغيل خادم MCP (Run MCP Server)

```bash
npm run mcp:start
```

---

## النشر (Deployment)

### الواجهة الأمامية (Frontend)

**GitHub Pages**:
1. انسخ محتويات `frontend/` إلى `docs/`
2. ادفع إلى GitHub
3. فعّل GitHub Pages في إعدادات المستودع
4. اختر مجلد `docs/` كمصدر

### الخادم (Backend)

**Render.com**:
1. ربط المستودع بـ Render
2. اختر خدمة Web Service
3. أمر البناء: `npm install`
4. أمر البدء: `npm start`
5. أضف متغيرات البيئة

### خادم MCP (MCP Server)

**GitHub Copilot**:
1. الملف `.github/copilot/mcp.json` جاهز
2. سيتم تحميل الخادم تلقائيًا بواسطة Copilot
3. تأكد من تثبيت التبعيات

---

## الأوامر المتاحة (Available Commands)

```bash
# الخادم (Server)
npm run dev              # تشغيل خادم التطوير
npm start                # تشغيل خادم الإنتاج

# التحقق (Validation)
npm test                 # تشغيل جميع عمليات التحقق
npm run validate         # التحقق من البيانات
npm run validate:registry # التحقق من السجل

# الصحة (Health)
npm run health           # فحص صحة أساسي
npm run health:detailed  # فحص صحة شامل

# MCP
npm run mcp:install      # تثبيت تبعيات MCP
npm run mcp:start        # تشغيل خادم MCP

# مراجعة PR
npm run pr-check         # مراجعة محلية لـ PR
npm run pr-check:verbose # مراجعة مفصلة
```

---

## الأمان (Security)

### الميزات الأمنية:
- ✅ HTTPS فقط في الإنتاج
- ✅ CORS مع أصول محددة
- ✅ Content Security Policy (CSP)
- ✅ حدود المعدل (Rate Limiting)
- ✅ Helmet.js للحماية
- ✅ Circuit Breaker لمقاومة الأعطال
- ✅ لا توجد مفاتيح API في الواجهة الأمامية
- ✅ مقارنة رموز آمنة من التوقيت

---

## التطوير (Development)

### إضافة وكيل جديد:

1. أضف تعريف الوكيل في `shared/config.js`
2. أنشئ نقطة نهاية API في `src/routes/`
3. أضف أداة MCP في `mcp-servers/bsu-agent-server.js`
4. أضف زر في الواجهة الأمامية `frontend/index.html`

### اختبار محلي:

```bash
# اختبر الخادم
npm run dev

# اختبر الواجهة الأمامية
cd frontend
python3 -m http.server 8000
```

---

## الترحيل من المستودع القديم (Migration from Legacy)

### خطوات الترحيل:

1. **استنساخ المستودع الجديد**:
   ```bash
   git clone https://github.com/MOTEB1989/BSM.git
   ```

2. **تحديث العناوين**:
   - استبدل `LexBANK/BSM` بـ `MOTEB1989/BSM` في جميع الملفات
   - حدّث روابط الواجهة الأمامية

3. **إعادة توجيه المستودع القديم**:
   - أضف ملف README في `LexBANK/BSM` يشير إلى المستودع الجديد
   - استخدم GitHub redirect أو archive

4. **تحديث النشر**:
   - حدّث إعدادات GitHub Pages
   - حدّث إعدادات Render.com

---

## الدعم (Support)

### الروابط المهمة:
- 📖 [التوثيق الكامل](https://github.com/MOTEB1989/BSM/blob/main/README.md)
- 🐛 [الإبلاغ عن مشكلة](https://github.com/MOTEB1989/BSM/issues)
- 💬 [المناقشات](https://github.com/MOTEB1989/BSM/discussions)

### للحصول على المساعدة:
1. راجع الوثائق في مجلد `docs/`
2. تحقق من ملفات README في كل مجلد
3. ابحث في القضايا المفتوحة
4. اطرح سؤالاً جديدًا

---

## الإصدار (Version)

- **الإصدار الحالي**: 2.0.0
- **تاريخ التوحيد**: فبراير 2026
- **الحالة**: نشط ومدعوم ✅

---

## الخلاصة (Summary)

تم توحيد منصة BSU/LexBANK بنجاح في مستودع واحد يحتوي على:
- ✅ واجهة أمامية موحدة (`frontend/`)
- ✅ خادم خلفي قوي (`src/`)
- ✅ تكامل MCP مع Copilot (`mcp-servers/`)
- ✅ تكوين مشترك (`shared/config.js`)
- ✅ توثيق شامل (`docs/`)

جميع المكونات تعمل معًا بسلاسة وتوفر تجربة موحدة للمستخدمين والمطورين.

---

**للمزيد من المعلومات**، راجع ملفات README في كل مجلد فرعي.
